/*import { render, screen } from '@testing-library/react'
import App from './App'

test('renders learn react link', () => {
  render(<App />) // create virtual DOM using @testing-library/react
  const linkElement = screen.getByText(/learn react/i)
  //Screen is a object which consist methods to query vertual DOM
  expect(linkElement).toBeInTheDocument() // toBeInTheDocument rfers virtual DOM created by render method.
}) */

// test , expect method is provided by create-react-app
// test and expect are methods from jest which create react app globally provides
